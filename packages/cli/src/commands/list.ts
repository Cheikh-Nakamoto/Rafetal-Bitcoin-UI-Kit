/**
 * `bitcoin-ui list` — fetches the registry index and prints all available
 * components grouped by category.
 *
 * Status: Phase 0 — stub.
 */

export async function list(): Promise<void> {
  throw new Error("TODO(phase-0): implement list")
}
