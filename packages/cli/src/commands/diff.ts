/**
 * `bitcoin-ui diff <name>` — compares the local copy of a component against
 * the registry version. Critical UX since users own the source.
 *
 * Status: Phase 0 — stub.
 */

export async function diff(component: string | undefined): Promise<void> {
  void component
  throw new Error("TODO(phase-0): implement diff")
}
