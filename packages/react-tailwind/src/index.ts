/**
 * @btc-ui/react-tailwind
 *
 * Reference Tailwind implementation of the headless primitives in
 * `@btc-ui/react`. Most users will not install this directly — they'll copy
 * components from the registry via the `bitcoin-ui` CLI. This package exists
 * for users who want a pinned, npm-installable version.
 */
export const __PHASE_0_PLACEHOLDER__ = true as const
