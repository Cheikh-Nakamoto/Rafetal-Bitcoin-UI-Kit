/**
 * Headless primitives. Phase 1 ships: Balance, Address, TXID, BlockHeight,
 * FiatAmount, ConfirmationsBadge, ExchangeRate, SatBtcSwitch.
 *
 * Status: Phase 0 — placeholders so the package builds.
 */

export const __PHASE_0_PLACEHOLDER__ = true as const
